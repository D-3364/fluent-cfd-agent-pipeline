# 项目工作约束

适用于在本仓库内的一切工作，**包括维护 harness 本身**。

> 本文件只写**规则**和**指针**，不写解释。解释在各自的事实源里，这里不复制 ——
> 复制就会分叉，本仓库已经吃过这个亏（见规则 1、2）。

---

## 这是什么

把 `网格文件 + 自然语言需求` 变成**经独立审查的** CFD 结果。三 agent 流水线
（定规范 / 执行 / 审查），产物落 `runs/<id>/`。

**跑任务**看 `.claude/skills/fluent-cfd-pipeline/SKILL.md`（skill 触发时自动加载）。
**本文件管的是"没有任务在跑"时的仓库维护工作** —— 那是 skill 不覆盖的另一半。

## 各部件在哪

| 要做什么 | 去哪 |
|---|---|
| 跑一个 CFD 任务（要独立审查） | `/fluent-run`，或直接描述需求（skill 自动触发） |
| 跑一个 CFD 任务（探索式，不需审查） | `/fluent-quick` —— 单 agent，省 token，**但结果只有自己检查** |
| 改流水线行为 / 调度逻辑 | `.claude/skills/fluent-cfd-pipeline/SKILL.md` |
| 改某个 agent 的职责或工具集 | `.claude/agents/cfd-*.md` |
| 查**实测过的**工具与 API 事实 | `references/mcp-tool-truths.md`（怎么跟 server 说话）、`references/solver-api-26.1.md`（怎么驱动求解） |
| 查审查阈值 | `references/review-criteria.md` |
| 环境接线 / 换机器 | `INSTALL.md` + `scripts/setup.py` |
| 验证环境可用性 | `scripts/` 下的分层脚本，见 `README.md` |
| 用户视角的说明 | `README.md` |

---

## 规则

### 1. ★ 加了东西就同步索引 —— 这条已犯过三次

新增 / 改名 / 移动**文档或产物**时，必须回头改**所有**索引它的地方。
实际发生过的漏改：

- 加第七份 reference → 忘了改 README 的「六份支撑文档」
- 加 `00_env.json` → 忘了登记进 SKILL.md 的产物契约表
- 修一处勘误 → 只改了正文，漏了 `examples/README.md` 的故障注入节

**提交前搜一遍**新名字在 `README.md` / `SKILL.md` / `INSTALL.md` 里的陈旧引用。
**宁可写"见下表"，也不要写硬编码的数目字** —— 数目字必漂。

### 2. ★ 事实的单一归属

每件事**只有一处写完整版**，别处缩成一行 + 指针。

重复已经开始，**新增内容时不要扩大它**：

- `512K` 说明在 `review-criteria.md` 与 `student-limits.md` 各一份
- `"Net 恒为 0"` 在两处逐字相同
- `solver-api-26.1.md` 把某算例的 zone 名 `interior-water-pipe` 写进了本应通用的规则

**例外**：agent 定义里的**操作规则**可以内联 —— subagent 每次全新派发，必须自足。
但那是"一行规则"，不是"完整解释"。判据：**规则内联，解释留指针。**

### 3. 提交风格

**主题行**：一句话说清**改了什么**。动词开头，陈述句。

```
✅ 加 Apache-2.0 许可与 NOTICE
✅ 修 examples 对 runs/ 的失效引用
✅ 清本机算例路径，README 补前置条件

❌ 发布前准备（二）          ← 过程标签，记的是"什么时候做的"
❌ 补 cwd —— 作者自己刚踩了这个坑   ← 作者叙事
❌ 修一处让读者去核对不存在的东西     ← 描述问题，不是改动
❌ INSTALL.md 去掉过度标记            ← 以文件名开头，见下
```

> ⚠️ **不要以文件名开头。** GitHub 的文件列表会在**文件名旁边**显示该文件最后
> 一次改动它的提交主题，于是 `INSTALL.md` 会显示成「INSTALL.md — INSTALL.md
> 去掉过度标记」，像绕口令。文件名已经在旁边了，主题行只需说改了什么。
>
> 同理，**一次提交尽量只做一件事**。捆了三件事的主题行显示在某个具体文件旁边时，
> 只有一句相关，另两句看起来像无关内容。

**正文**：**为什么**（附证据）→ **改动** → **已验证** / **未验证**。

未验证的单独成节，不许混进"已验证"。

> ⚠️ **「为什么」写在正文，不要写进主题行。** 本仓库出现过连续三笔以
> 「发布前准备（一/二/三）」开头的提交——那是把工作的时间顺序当成了提交语义，
> 事后没人能从中看出改了什么。

### 4. 验证诚实

**未跑过的不许说成"验证完成"。推测要标"推测"。错误要如实报告并附输出。**

这条是整个项目的地基 —— 审查 agent 的 `cannot_verify` 机制是同一个原则。

> ⚠️ **尤其不要把「未经查证的原因」写进文档当结论。** 本仓库出过两次：
>
> - `fluent-run.md` 曾写「实测发生过：以编程方式调用时位置参数可能不替换」，
>   把症状归因成调用方式。**真因是参数下标 0-based**，`$1` 本来就拿第二个参数。
>   那条错误的"实测记录"留在文件里，掩盖了真 bug 很久。
> - `AGENTS.md` 曾把 `setup.py --check` 说成能验证"会话有没有加载 MCP"，
>   实际它只查磁盘配置。
>
> **写「原因」之前先证到它。** 证不到就写「现象是 X，原因未查明」——
> 一个未查证的解释比没有解释更有害，因为它会让人停止怀疑。

### 5. 平台与路径

- 脚本按 **Windows** 写（`.venv/Scripts/`、`tasklist`）。换平台需适配。
- **非 ASCII 路径会让 Fluent 崩，且报错指向网格**（看起来像网格损坏，实际是目录名）。
  动路径相关代码前先读 `references/mcp-tool-truths.md#9`。

### 6. 动手前先核，别照文档字面推

本项目已多次出现"文档与实际不符"。改之前先实测或核原文。近例：

- MCP 帮助文本把 `iter_count` 描述成 "Incremental number of time steps" —— **是错的**
- `examples/README.md` 顶部的勘误块与下方正文**互相矛盾**（同一份文件里）

**文档本身也是被审查对象。** 发现文档错了，改它，并在提交信息里说明为什么之前是错的。
